@model Moov2.OrchardCore.Fields.ResponsiveMedia.ViewModels.EditResponsiveMediaFieldViewModel

@using OrchardCore.ContentManagement.Metadata.Models
@using Moov2.OrchardCore.Fields.ResponsiveMedia.Settings

@{
    var settings = Model.PartFieldDefinition.Settings.ToObject<ResponsiveMediaFieldSettings>();
}

<fieldset class="form-group">
    <label asp-for="Data">@Model.PartFieldDefinition.DisplayName()</label>
    <textarea asp-for="Data" rows="3" class="form-control content-preview-text"></textarea>

    @if (!String.IsNullOrEmpty(settings.Hint))
    {
        <span class="hint">@settings.Hint</span>
    }
</fieldset>